# ── Python ────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/

# ── Large training datasets (download separately) ─────────
# Keep small test/val sets for reproducibility; exclude big training files
datasets/full.csv
datasets/bindingdb/full.csv
datasets/random/train.csv
datasets/bindingdb/random/train.csv
datasets/cluster/source_train.csv
datasets/bindingdb/cluster/source_train.csv
datasets/cluster/target_train.csv
datasets/bindingdb/cluster/target_train.csv

# ── Screening output (generated — not tracked) ────────────
screening/output/

# ── Generated prediction output ───────────────────────────
last_prediction.txt
predictions.csv
predict_results_*.csv
compare_results_*.csv
chembl_prep.log

# ── Large external datasets (not tracked) ────────────────
chembl-dataset/
chembl-dataset.tar.gz
*_pairs.csv
*_pairs_pred*.csv
chembl_*.csv
chembl_*.log

# ── Container build artifacts (large / machine-local) ─────
*.sif
*.simg
.singularity/
build_sif.log

# ── OS / editor ───────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
.venv/
.env

# ── Slurm job logs / test artifacts ───────────────────────
teiban_*.log
slurm_test_*
teiban_chunks_*/

# ── Private / unpublished manuscript — NEVER publish ───────
# Contains the manuscript, cover letter, and suggested reviewer names.
B1_sci_adv/

# ── Local experiment outputs (not part of released software) ─
results/
fda_validation/*.log
fda_validation/davis_results*.csv
fda_validation/ponatinib_results.csv
